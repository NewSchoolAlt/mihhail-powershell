if (-not ([AppDomain]::CurrentDomain.GetAssemblies() | Where-Object { $_.GetName().Name -eq 'WebDriver' })) { [Reflection.Assembly]::LoadFrom((Split-Path $MyInvocation.MyCommand.Path) + '\WebDriver.dll') }; $driver = [OpenQA.Selenium.Chrome.ChromeDriver]::new((Split-Path $MyInvocation.MyCommand.Path)); $driver.Navigate().GoToUrl('https://example.com'); $title = $driver.Title; Write-Host '--------------'; Write-Host $title; Write-Host '--------------'; Start-Sleep -Seconds 2; $driver.Quit()
